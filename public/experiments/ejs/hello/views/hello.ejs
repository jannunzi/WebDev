<h1>Hello</h1>

<p>
    This HTML template is rendered
    on the server using EJS. The
    following route maps URL
    <code>/hello</code> to this
    EJS file <code>hello.ejs</code>
    in the <code>views</code>
    directory
</p>

<pre>
// load express
var express = require('express');
var app = express();

// use ejs as rendering engine
app.set('view engine', 'ejs');

// map /hello to this file (.ejs is optional)
app.get('/hello', function(req, res) {
    res.render('hello');
});

// listen for http requests
app.listen(8080);
</pre>